name: Java CI with Maven

on:
  pull_request:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: set up JDK 17
        uses: actions/setup-java@v1
        with:
          java-version: 17

      - name: Setup Postgres database
        uses: Daniel-Marynicz/postgresql-action@master
        with:
          postgres_image_tag: 16.0
          app_user: admin
          app_user_password: admin
          app_db: inspection-service-sb

      - name: Maven Package
        run: mvn -B clean package -DskipTests

  #  codestyle:
  #    runs-on: ubuntu-latest
  #    steps:
  #      - name: Install CodeClimate CLI
  #        run: gem install codeclimate
  #
  #      - name: Run CodeClimate analysis
  #        run: codeclimate analyze --all
  #
  #      - name: Upload results to CodeClimate
  #        run: codeclimate format-coverage -t lcov coverage/.resultset.json

  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Code Climate
        uses: remarkablemark/setup-codeclimate@v2

      - uses: remarkablemark/setup-codeclimate@v2
      - run: |
          cc-test-reporter before-build
          npm test
          cc-test-reporter after-build --exit-code $?
        env:
          CC_TEST_REPORTER_ID: ${{ secrets.CC_TEST_REPORTER_ID }}