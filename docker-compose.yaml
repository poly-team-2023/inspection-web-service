version: "3.9"
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_DB: "inspection-service-sb"
      POSTGRES_USER: "admin"
      POSTGRES_PASSWORD: "admin"
    ports:
      - "5432:5432"